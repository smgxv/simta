sonar.organization=timothy-source
sonar.projectKey=timothy-source_secure-simta
sonar.projectName=Secure SIMTA
sonar.host.url=https://sonarcloud.io
sonar.login=${SONAR_TOKEN}

# Semua folder service dianalisis
sonar.sources=ta_service, user_service, document_service

# Kecualikan folder yang tidak relevan
sonar.exclusions=**/tests/**,**/vendor/**

# Jika ada laporan coverage Go bisa tambahkan:
# sonar.go.coverage.reportPaths=coverage.out
